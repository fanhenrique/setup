set $monitor1 "DP-1"
set $monitor2 "DP-2"

set $monitors $monitor1 $monitor2

### Output configuration
#
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
# This is commented in Debian, because the Sway wallpaper files are in a separate
# package `sway-backgrounds`. Installing this package drops a config file to
# /etc/sway/config.d/
# output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill
#
# Example configuration:
#
output $monitor1 resolution 1920x1080 position 0,0
output $monitor2 resolution 1440x900 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs

# Brightness monitors
# https://github.com/fanhenrique/monitorctl
bindsym $mod+comma exec brightness --down --step 0.1 --monitor $monitors
bindsym $mod+period exec brightness --up --step 0.1 --monitor $monitors
bindsym $mod+m exec brightness --reset --monitor $monitors

# Red shift monitors
# redshift installation required
# sudo apt install gammastep
bindsym $mod+slash exec gammastep -O 5500
